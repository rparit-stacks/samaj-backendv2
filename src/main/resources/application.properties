spring.application.name=samaj
server.port=9512

#spring.datasource.url=jdbc:postgresql://13.60.97.231:5432/samaj_app
spring.datasource.url=jdbc:postgresql://ep-late-dust-an5m61dh-pooler.c-6.us-east-1.aws.neon.tech:5432/samaj_user?sslmode=require

spring.datasource.username=neondb_owner
spring.datasource.password=npg_JWXwicD2dQ8U

spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.jpa.show-sql=false

samaj.jwt.secret=gerzxLnyO4tlY1TJYSkEg1s7YIRTbggLBwgYYlT1s8fC5EX8qLx4Fh1HexQYuATj
# Access token: long enough to avoid constant refresh noise; refresh token keeps sessions alive (10+ days).
samaj.jwt.access-ttl-minutes=1440
samaj.jwt.refresh-ttl-days=30

samaj.storage.provider=LOCAL
samaj.storage.root=./data/samaj-uploads
samaj.storage.public-base-url=http://localhost:8080/files
samaj.storage.s3.bucket=
samaj.storage.s3.region=ap-south-1
samaj.storage.s3.access-key-id=
samaj.storage.s3.secret-access-key=
samaj.storage.s3.endpoint=

# Bootstrap parent admin (created on startup if email+password set and user missing)
samaj.bootstrap.parent-admin.email=admin@samajuser.com
samaj.bootstrap.parent-admin.password=Rparit@111288

# Dev only: send X-User-Id and X-Admin-User-Id (UUID) to call user/admin APIs without JWT
samaj.dev.permissive-auth=true

# Optional extra CORS origin patterns (e.g. custom domain apex + www — they count as two origins).
# samaj.cors.additional-origin-patterns[0]=https://www.example.com
# samaj.cors.additional-origin-patterns[1]=https://example.com

# Matrimony external chat bridge (optional). If empty, webhook rejects all calls.
samaj.matrimony.webhook-secret=

# SMTP Configuration (can be overridden in admin settings)
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# OTP Configuration
samaj.otp.ttl-minutes=10
samaj.otp.max-attempts=5

logging.level.org.springframework.security=INFO

# Cloudinary (optional). Set environment variable CLOUDINARY_URL — never commit real credentials in this file.
samaj.cloudinary.url=cloudinary://988261371328627:5JNztGOWVc5e7gsWespVYb8e2bg@dm7eyzc0c

spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=25MB
